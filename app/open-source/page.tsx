import { OpenSource } from "@/components/sections/open-source";

export const metadata = { title: "Open source & protocol engineering" };

export default function Page() {
  return (
    <main>
      <OpenSource />
    </main>
  );
}
